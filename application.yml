spring:
  cloud:
    consul:
      config:
        enabled: true
      discovery:
#        tells the system to use IP rather than host name for discovery.
        prefer-ip-address: true
#      host: 127.0.0.1
#      port: 8500